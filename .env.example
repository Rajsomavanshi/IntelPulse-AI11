# IntelPulse AI currently uses public evidence APIs and needs no API key.
# Keep future provider credentials server-side only.
NODE_ENV=development
# DATABASE_URL=postgresql://user:password@host:5432/database